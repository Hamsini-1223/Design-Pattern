/**
 * Defines a strategy for teaching a subject.
 * Implementations of this interface can provide different teaching approaches.
 */

package Strategy.strategy;

public interface TeachingStrategy {
    void teach();
}
